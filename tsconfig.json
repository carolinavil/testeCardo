{
  "compilerOptions": {
    "target": "ES6",
    "module": "commonjs",
    "outDir": "./dist",  // Certifique-se de que o diretório de saída está correto
    "rootDir": "./src",  // A pasta de origem onde os arquivos .ts estão localizados
    "strict": true
  },
  "include": [
    "src/**/*.ts"
  ]
}
